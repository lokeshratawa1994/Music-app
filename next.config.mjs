// next.config.mjs

const nextConfig = {
    reactStrictMode: true,
    images: {
      domains: ['chillhop.com','source.unsplash.com'],
    },
    // Add other configurations as needed
  };
  
  export default nextConfig;
  